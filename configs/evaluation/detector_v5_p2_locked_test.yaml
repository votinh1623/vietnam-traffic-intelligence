schema_version: 1
evaluation_id: yolov8s_v5_p2_locked_test_v1
status: frozen_final_test

model:
  weights: runs/detect/runs/detect/research/yolov8s_v5_seed0_p2_continued/weights/best.pt
  expected_sha256: aac1d2bf703698e7fcf8e5af8f942028f70e7f0e3fc996c59dd480206ee889d2

dataset:
  data_yaml: datasets/vietnam_dataset_v5/data.yaml
  materialization_manifest: datasets/vietnam_dataset_v5/manifest.csv
  test_lock: manifests/datasets/vietnam_v5/test_lock.json
  expected_test_lock_sha256: 6a9c275059591b7a1ae410b6a813e9c2041beac068801a37e44db5d432ebc1d3
  split: test

validation:
  imgsz: 1280
  batch: 2
  device: "0"
  workers: 0
  plots: true

provenance:
  require_clean_worktree: true
  output_root: benchmark_outputs/detector_v5_p2_locked_test
  claim_boundary: >-
    One final evaluation of the yolov8s_v5_seed0_p2_continued checkpoint (P2
    stride-4 detection-head ablation, best epoch by validation mAP50-95, from
    a two-stage/restarted fine-tune -- see
    experiments/yolov8s_v5_seed0_p2_continued_20260819T152917/run.json) on
    the same content-addressed locked test used for yolov8s_v5_locked_test_v1
    (baseline) and yolov8s_v5_nwd_locked_test_v1 (NWD). Unlike the baseline,
    this checkpoint's architecture was designed with knowledge of this exact
    locked test's box-size distribution (median 16px), so this result is
    exploratory, not a blind confirmatory test -- same caveat as the NWD
    ablation. These results must not be used to select or tune checkpoints,
    thresholds, augmentations, or backends, and this locked test is consumed
    only once for this model, same as the baseline and NWD.
