name: traffic
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10.20
  - pytorch=2.6.0
  - torchvision=0.21.0
  - pytorch-cuda=12.4
  - numpy=2.2.6
  - pandas=2.3.3
  - pyyaml=6.0.3
  - pip
  - pip:
      - ultralytics==8.4.115
      - opencv-python==5.0.0.93
      - motmetrics==1.4.0
      - transformers==5.14.1
      - sahi==0.12.2
      - pycocotools==2.0.11

# This records the audited environment; it has not been recreated in this
# phase. ONNX Runtime, TensorRT, TrackEval, and quantization runtimes are added
# only after a backend-specific dependency proposal is approved.
